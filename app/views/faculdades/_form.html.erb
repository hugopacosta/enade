<%= form_with(model: faculdade, local: true) do |form| %>
  <% if faculdade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(faculdade.errors.count, "error") %> prohibited this faculdade from being saved:</h2>

      <ul>
      <% faculdade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :faculdade_nome %>
  </div>

  <div class="field">
    <%= form.label :nota_geral %>
    <%= form.text_field :nota_geral, id: :faculdade_nota_geral %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
