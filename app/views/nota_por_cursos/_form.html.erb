<%= form_with(model: nota_por_curso, local: true) do |form| %>
  <% if nota_por_curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nota_por_curso.errors.count, "error") %> prohibited this nota_por_curso from being saved:</h2>

      <ul>
      <% nota_por_curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :faculdade_id %>
    <%= form.collection_select :faculdade_id, Faculdade.order(:nome), :id, :nome, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label :curso_id %>
    <%= form.collection_select :curso_id, Curso.order(:nome_do_curso), :id, :nome_do_curso, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label :nota %>
    <%= form.text_field :nota, id: :nota_por_curso_nota %>
  </div>

  <div class="field">
    <%= form.label :media_alunos %>
    <%= form.text_field :media_alunos, id: :nota_por_curso_media_alunos %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
